<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.smarthome.mapper.GoodsMapper" >
  <resultMap id="BaseResultMap" type="com.example.smarthome.admin.Goods" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="goodsname" property="goodsname" jdbcType="VARCHAR" />
    <result column="equ_id" property="equId" jdbcType="INTEGER" />
    <result column="createtime" property="createtime" jdbcType="TIMESTAMP" />
    <result column="price" property="price" jdbcType="INTEGER" />
    <result column="equname" property="equname" jdbcType="VARCHAR"></result>
  </resultMap>
  <sql id="Base_Column_List" >
    id, goodsname, equ_id, createtime, price
  </sql>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select 
    <include refid="Base_Column_List" />
    from goods
    where id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from goods
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.example.smarthome.admin.Goods" >
    <selectKey resultType="java.lang.Integer" keyProperty="id" order="AFTER" >
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into goods (goodsname, equ_id, createtime, 
      price)
    values (#{goodsname,jdbcType=VARCHAR}, #{equId,jdbcType=INTEGER}, #{createtime,jdbcType=TIMESTAMP}, 
      #{price,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.example.smarthome.admin.Goods" >
    <selectKey resultType="java.lang.Integer" keyProperty="id" order="AFTER" >
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into goods
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="goodsname != null" >
        goodsname,
      </if>
      <if test="equId != null" >
        equ_id,
      </if>
      <if test="createtime != null" >
        createtime,
      </if>
      <if test="price != null" >
        price,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="goodsname != null" >
        #{goodsname,jdbcType=VARCHAR},
      </if>
      <if test="equId != null" >
        #{equId,jdbcType=INTEGER},
      </if>
      <if test="createtime != null" >
        #{createtime,jdbcType=TIMESTAMP},
      </if>
      <if test="price != null" >
        #{price,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.example.smarthome.admin.Goods" >
    update goods
    <set >
      <if test="goodsname != null" >
        goodsname = #{goodsname,jdbcType=VARCHAR},
      </if>
      <if test="equId != null" >
        equ_id = #{equId,jdbcType=INTEGER},
      </if>
      <if test="createtime != null" >
        createtime = #{createtime,jdbcType=TIMESTAMP},
      </if>
      <if test="price != null" >
        price = #{price,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.example.smarthome.admin.Goods" >
    update goods
    set goodsname = #{goodsname,jdbcType=VARCHAR},
      equ_id = #{equId,jdbcType=INTEGER},
      createtime = #{createtime,jdbcType=TIMESTAMP},
      price = #{price,jdbcType=INTEGER}
    where id = #{id,jdbcType=INTEGER}
  </update>
  <resultMap extends="BaseResultMap" type="com.example.smarthome.admin.Goods" id="hotGoods">
    <collection property="photos" ofType="com.example.smarthome.admin.Photo">
      <id column="pid" property="id"></id>
      <result column="photo" property="imgurl"></result>
    </collection>

  </resultMap>
  <select id="hotGoods" resultMap="hotGoods" >
    SELECT g.id as id,g.goodsname as goodsname,g.createtime as createtime,g.price as price,e.equname as equname,p.id as pid,p.imgurl as photo from goods g
LEFT JOIN equipment e
on g.equ_id=e.id
LEFT JOIN photo p
on g.id=p.goods_id
limit #{pageNum},#{pageSize}
  </select>
  <select id="findByEid" resultType="map">
select gt.gtname,p.shu,goodsname,g.price,pp.imgurl from goodstype gt
INNER join (SELECT gt_id,sum(count) as shu from orders GROUP BY gt_id) as p
on gt.id=p.gt_id
left JOIN goods g
on gt.goods_id=g.id
LEFT JOIN photo pp
on g.id=pp.goods_id
where equ_id=#{eid}
ORDER BY shu DESC
limit 0,10
  </select>

  <select id="findById" resultMap="hotGoods">
   select g.*,p.id as pid,p.imgurl as photo from goods g
   left join photo p
   on g.id=p.goods_id
   where p.goods_id=#{id}
  </select>
</mapper>